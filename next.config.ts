import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "via.placeholder.com",
      "picsum.photos",
      "ui-avatars.com",
      "placehold.co",
      "api.dicebear.com",
    ],
  },
};

export default nextConfig;
